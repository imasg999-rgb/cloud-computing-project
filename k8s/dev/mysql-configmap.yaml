apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init-config-dev
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS crud_db;
    USE crud_db;

    CREATE TABLE IF NOT EXISTS items (
        id INT AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(255) NOT NULL
    );